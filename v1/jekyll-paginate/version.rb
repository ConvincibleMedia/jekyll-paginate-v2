module Jekyll
  module Paginate
    # Version of the legacy `jekyll-paginate` code shipped for compatibility.
    #
    # Used by legacy entry points and packaging metadata.
    VERSION = "1.1.0"
  end
end
